# Database (test instance on port 5433)
DATABASE_URL="postgresql://postgres:postgres@localhost:5433/movie_db_test?schema=public"

# JWT
JWT_SECRET="test-jwt-secret-key"

# Redis
REDIS_HOST="localhost"
REDIS_PORT=6379

# Notifications disabled in tests (no API keys)
SENDGRID_API_KEY=
TWILIO_ACCOUNT_SID=
